# vidcut M1「看得到」Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 vidcut 的地基：ProjectStore（唯一真相來源）+ WebSocket 同步 + ffmpeg ingest 管線 + React 唯讀時間軸 + 雙 `<video>` 無縫預覽播放器；手寫 project.json 後 UI 能無縫播放多段 proxy 並疊加 overlay/字幕。

**Architecture:** 單一 Node+TS 程序（Express 5，綁 `127.0.0.1:3845`）持有 project.json，透過 immer `produceWithPatches` 做序列化 mutation 並經 WS 廣播 patch；React UI（Vite dev server 代理到後端）以 zustand 儲存文件副本、`applyPatches` 增量更新；預覽不渲染——A/B 兩個 `<video>` 元素播 proxy 檔交替切換，overlay/字幕為絕對定位 DOM。詳見 spec：`docs/superpowers/specs/2026-07-29-ai-video-editor-design.md`。

**Tech Stack:** Node 22、TypeScript（strict, ESM）、Express 5、`ws`、`immer`、React 19、Vite、zustand、vitest、原生 ffmpeg/ffprobe（Homebrew）。

**本計畫是 spec 里程碑 M1（共 4 個里程碑，M2–M4 於 M1 驗收後各自出計畫）。**

## Global Constraints

- Server 只綁 `127.0.0.1`，port `3845`（spec §2.1）。
- 時間單位一律秒（浮點）；UI 顯示層才換算幀（spec §3）。
- 專案畫布固定 `1080×1920 @ 30fps`（spec §3）。
- proxy 轉檔參數必須完全一致（spec §8.1）：`scale=-2:960,fps=30` + `libx264 -preset veryfast -crf 23 -g 15 -keyint_min 15 -sc_threshold 0 -tune fastdecode -pix_fmt yuv420p` + `aac -b:a 128k -ac 2` + `-movflags +faststart`；無音軌素材補 `anullsrc` 靜音軌。
- 任何 ffmpeg 剪切/轉檔一律重新編碼，禁用 `-c copy`（spec §8.2）。
- 禁止引入：Remotion、`@designcombo/*`、Twick（spec §2.3 授權紅線）。允許參考 OpenCut classic / omniclip（MIT）。
- 所有專案狀態變更必須走 `ProjectStore.mutate()`；不得旁路直改 doc（spec §2.1 核心原則）。
- JSON 不能存 `Infinity`：overlay 的「到片尾」以 `duration: null` 表示（spec §3 的 `Infinity` 落地為 `null`）。
- 全部套件用 npm workspaces（root `package.json` workspaces: `shared`, `server`, `ui`）；測試一律 vitest。
- Commit 訊息格式：`feat(m1): ...` / `test(m1): ...` / `chore(m1): ...`。

## File Structure（M1 全貌）

```
ai-video-cut/
  package.json                  # workspaces root；scripts: test / dev
  tsconfig.base.json            # strict ESM 共用設定
  shared/                       # @vidcut/shared —— 型別 + 純函數（server/ui 共用）
    src/types.ts                #   Project 與各 Item 型別、WS 訊息型別、createEmptyProject
    src/timeline.ts             #   純時間軸計算：totalDuration / clipStartTimes / locate / overlayWindow
  server/                       # @vidcut/server
    src/store.ts                #   ProjectStore：load/save(原子+debounce)/mutate/undo/onChange
    src/ffmpeg.ts               #   run(ffmpeg args) / probe(file)
    src/ingest.ts               #   ingestMedia：proxy + filmstrip + peaks + 登記進 store
    src/app.ts                  #   createApp(store, projectDir)：靜態 UI、/media、/api/*
    src/wsHub.ts                #   attachWs(httpServer, store)：full/patch 協議
    src/index.ts                #   CLI 入口：vidcut serve <projectDir>
    test/fixtures.ts            #   ffmpeg lavfi 產測試影片
    test/*.test.ts
  ui/                           # @vidcut/ui
    vite.config.ts              #   dev proxy → 127.0.0.1:3845
    src/main.tsx / App.tsx
    src/ws.ts                   #   WS client：resync、指數退避重連
    src/stores/project.ts       #   zustand：doc/version、applyPatches
    src/stores/playback.ts      #   zustand：time/playing/seek
    src/player/plan.ts          #   純函數 planAt：時間 → active/next/overlays/captions
    src/player/Player.tsx       #   A/B video 引擎 + overlay/caption DOM 層
    src/timeline/Timeline.tsx   #   唯讀時間軸：尺規、clip(filmstrip/波形)、playhead、點擊 seek
    src/timeline/scale.ts       #   純函數 px↔時間換算
    src/stores/project.test.ts、src/player/plan.test.ts、src/timeline/scale.test.ts
  projects/demo/                # Task 12 的 fixture 專案（gitignore media 產物）
```

---

### Task 1: Workspace 骨架 + shared 型別

**Files:**

- Create: `package.json`, `tsconfig.base.json`, `shared/package.json`, `shared/tsconfig.json`, `shared/src/types.ts`, `shared/src/index.ts`
- Test: `shared/src/types.test.ts`

**Interfaces:**

- Consumes: 無（第一個任務）。
- Produces: `@vidcut/shared` 匯出——spec §3 全部介面（`Project`, `MediaAsset`, `VideoClip`, `OverlayItem`, `CaptionItem`, `AudioItem`, `ReviewState`, `RenderState`）、WS 訊息型別（`WsServerMsg`, `WsClientMsg`）、`createEmptyProject(id, name): Project`。**後續所有任務 import 這些名字。**

- [ ] **Step 1: 建立 workspace 設定檔**

`package.json`（root）：

```json
{
  "name": "vidcut",
  "private": true,
  "type": "module",
  "workspaces": ["shared", "server", "ui"],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "dev:server": "npm run dev -w @vidcut/server",
    "dev:ui": "npm run dev -w @vidcut/ui"
  }
}
```

`tsconfig.base.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "verbatimModuleSyntax": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

`shared/package.json`：

```json
{
  "name": "@vidcut/shared",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "vitest run" },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^3.0.0" }
}
```

（workspace 內直接以 TS 原始碼互相引用；server 用 `tsx` 執行、ui 用 Vite，都吃得下 TS 進口，不需要 build step。）

`shared/tsconfig.json`：

```json
{ "extends": "../tsconfig.base.json", "include": ["src"] }
```

- [ ] **Step 2: 寫失敗測試**

`shared/src/types.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { createEmptyProject } from './types.js';

describe('createEmptyProject', () => {
  it('builds a schema-v1 project with fixed canvas and empty tracks', () => {
    const p = createEmptyProject('p1', 'demo');
    expect(p.schemaVersion).toBe(1);
    expect(p.canvas).toEqual({ width: 1080, height: 1920, fps: 30 });
    expect(p.tracks.video).toEqual([]);
    expect(p.tracks.overlays).toEqual([]);
    expect(p.tracks.captions).toEqual([]);
    expect(p.tracks.audio).toEqual([]);
    expect(p.review).toBeNull();
    expect(p.render.status).toBe('idle');
    expect(JSON.parse(JSON.stringify(p))).toEqual(p); // JSON-safe（無 Infinity/undefined）
  });
});
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `cd /Users/maohua/Desktop/gi_+repo/ai-video-cut && npm install && npm run test -w @vidcut/shared`
Expected: FAIL（`createEmptyProject` 不存在）

- [ ] **Step 4: 實作 `shared/src/types.ts`**

```ts
// spec §3 資料模型的逐字落地。時間單位一律秒（浮點）。
export interface ProbeInfo {
  duration: number;
  width: number;
  height: number;
  fps: number;
  hasAudio: boolean;
  rotation: number;
}

export interface MediaAsset {
  id: string;
  path: string; // 相對專案資料夾
  proxyPath?: string;
  filmstripPath?: string;
  peaksPath?: string;
  probe: ProbeInfo;
  label?: string;
  meta?: Record<string, unknown>;
}

export interface VideoClip {
  id: string;
  mediaId: string;
  in: number; // 來源內起點（= 舊腳本 ss）
  duration: number; // = 舊腳本 t
  label?: string;
  volume: number; // 0–2，預設 1
  meta?: Record<string, unknown>;
}

export interface OverlayItem {
  id: string;
  imagePath: string;
  anchor?: { clipId: string; offset: number }; // 與 start 二選一
  start?: number;
  duration: number | null; // null = 到片尾（JSON 不能存 Infinity）
  position: { x: number; y: number; scale: number }; // x,y 為 0–1 相對畫布
}

export interface CaptionItem {
  id: string;
  text: string;
  start: number;
  duration: number;
  style: { fontFamily: string; fontSize: number; fill: string; stroke?: string; y: number };
}

export interface AudioItem {
  id: string;
  mediaId: string;
  start: number;
  in: number;
  duration: number;
  volume: number;
  ducking?: boolean;
}

export interface ReviewState {
  id: string;
  summary: string;
  focus?: string[];
  sinceVersion: number;
  requestedAt: string;
}

export interface RenderState {
  lastOutput?: string;
  status: 'idle' | 'running' | 'done' | 'error';
  progress?: number;
}

export interface Project {
  schemaVersion: 1;
  id: string;
  name: string;
  canvas: { width: number; height: number; fps: number };
  media: MediaAsset[];
  tracks: {
    video: VideoClip[];
    overlays: OverlayItem[];
    captions: CaptionItem[];
    audio: AudioItem[];
  };
  review: ReviewState | null;
  render: RenderState;
}

// ---- WS 協議（spec §4.1）----
// patch 用 immer 的 Patch 形狀（{op, path: (string|number)[], value?}），UI 端 applyPatches 直接可用
export interface JsonPatch {
  op: 'add' | 'replace' | 'remove';
  path: (string | number)[];
  value?: unknown;
}

export type MutationSource = 'ai' | 'human';

export type WsServerMsg =
  | { type: 'full'; version: number; doc: Project }
  | { type: 'patch'; version: number; patches: JsonPatch[]; source: MutationSource; label: string };

export type WsClientMsg = { type: 'resync' };

export function createEmptyProject(id: string, name: string): Project {
  return {
    schemaVersion: 1,
    id,
    name,
    canvas: { width: 1080, height: 1920, fps: 30 },
    media: [],
    tracks: { video: [], overlays: [], captions: [], audio: [] },
    review: null,
    render: { status: 'idle' },
  };
}
```

`shared/src/index.ts`：

```ts
export * from './types.js';
export * from './timeline.js';
```

（`timeline.ts` 是 Task 2；此刻先建一個空檔 `export {};` 讓 import 不炸。）

- [ ] **Step 5: 跑測試確認通過**

Run: `npm run test -w @vidcut/shared`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add package.json tsconfig.base.json shared
git commit -m "feat(m1): workspace scaffold + shared project types"
```

---

### Task 2: 純時間軸計算（`shared/src/timeline.ts`）

**Files:**

- Create: `shared/src/timeline.ts`
- Modify: `shared/src/index.ts`（已 re-export，無需改）
- Test: `shared/src/timeline.test.ts`

**Interfaces:**

- Consumes: `Project`, `VideoClip`, `MediaAsset`, `OverlayItem`（Task 1）。
- Produces（UI 播放器、時間軸、之後 M4 渲染器都吃這組）：
  - `totalDuration(p: Project): number` — 主軌總長（磁性：clip duration 總和）
  - `clipStartTimes(p: Project): number[]` — 每個 clip 在時間軸上的起點
  - `locate(p: Project, t: number): Located | null`，`Located = { clipIndex: number; clip: VideoClip; media: MediaAsset; offsetInClip: number }`；t 超出範圍回 null；t 恰在邊界歸屬右側 clip；片尾 t === totalDuration 回最後一個 clip 的尾端
  - `overlayWindow(p: Project, o: OverlayItem): { start: number; end: number } | null` — 解析 anchor/start 與 `duration:null`（到片尾）；anchor 指向不存在的 clip 回 null

- [ ] **Step 1: 寫失敗測試**

`shared/src/timeline.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { createEmptyProject, type Project } from './types.js';
import { totalDuration, clipStartTimes, locate, overlayWindow } from './timeline.js';

function proj(): Project {
  const p = createEmptyProject('p', 'test');
  p.media = [
    {
      id: 'm1',
      path: 'a.mp4',
      probe: { duration: 20, width: 540, height: 960, fps: 30, hasAudio: true, rotation: 0 },
    },
    {
      id: 'm2',
      path: 'b.mp4',
      probe: { duration: 30, width: 540, height: 960, fps: 30, hasAudio: true, rotation: 0 },
    },
  ];
  p.tracks.video = [
    { id: 'c1', mediaId: 'm1', in: 5, duration: 6, volume: 1 },
    { id: 'c2', mediaId: 'm2', in: 10, duration: 4, volume: 1 },
  ];
  return p;
}

describe('timeline math', () => {
  it('totalDuration sums clip durations', () => {
    expect(totalDuration(proj())).toBe(10);
    expect(totalDuration(createEmptyProject('x', 'x'))).toBe(0);
  });

  it('clipStartTimes are cumulative', () => {
    expect(clipStartTimes(proj())).toEqual([0, 6]);
  });

  it('locate maps timeline time to clip + offset', () => {
    const p = proj();
    expect(locate(p, 0)).toMatchObject({ clipIndex: 0, offsetInClip: 0 });
    expect(locate(p, 5.5)).toMatchObject({ clipIndex: 0, offsetInClip: 5.5 });
    expect(locate(p, 6)).toMatchObject({ clipIndex: 1, offsetInClip: 0 }); // 邊界歸右側
    expect(locate(p, 9.9)?.clip.id).toBe('c2');
    expect(locate(p, 10)).toMatchObject({ clipIndex: 1, offsetInClip: 4 }); // 片尾特例
    expect(locate(p, 10.01)).toBeNull();
    expect(locate(p, -0.1)).toBeNull();
  });

  it('overlayWindow resolves anchors and null duration', () => {
    const p = proj();
    expect(
      overlayWindow(p, {
        id: 'o1',
        imagePath: 'x.png',
        start: 1,
        duration: 3,
        position: { x: 0, y: 0, scale: 1 },
      }),
    ).toEqual({ start: 1, end: 4 });
    expect(
      overlayWindow(p, {
        id: 'o2',
        imagePath: 'x.png',
        start: 2,
        duration: null,
        position: { x: 0, y: 0, scale: 1 },
      }),
    ).toEqual({ start: 2, end: 10 });
    expect(
      overlayWindow(p, {
        id: 'o3',
        imagePath: 'x.png',
        anchor: { clipId: 'c2', offset: 0.5 },
        duration: 2,
        position: { x: 0, y: 0, scale: 1 },
      }),
    ).toEqual({ start: 6.5, end: 8.5 });
    expect(
      overlayWindow(p, {
        id: 'o4',
        imagePath: 'x.png',
        anchor: { clipId: 'nope', offset: 0 },
        duration: 2,
        position: { x: 0, y: 0, scale: 1 },
      }),
    ).toBeNull();
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/shared`
Expected: FAIL（timeline 函數不存在）

- [ ] **Step 3: 實作 `shared/src/timeline.ts`**

```ts
import type { MediaAsset, OverlayItem, Project, VideoClip } from './types.js';

export interface Located {
  clipIndex: number;
  clip: VideoClip;
  media: MediaAsset;
  offsetInClip: number;
}

export function totalDuration(p: Project): number {
  return p.tracks.video.reduce((s, c) => s + c.duration, 0);
}

export function clipStartTimes(p: Project): number[] {
  const starts: number[] = [];
  let t = 0;
  for (const c of p.tracks.video) {
    starts.push(t);
    t += c.duration;
  }
  return starts;
}

export function locate(p: Project, t: number): Located | null {
  if (t < 0) return null;
  const total = totalDuration(p);
  if (p.tracks.video.length === 0 || t > total) return null;
  const starts = clipStartTimes(p);
  if (t === total) {
    // 片尾特例：回最後一個 clip 的尾端
    const i = p.tracks.video.length - 1;
    return located(p, i, p.tracks.video[i].duration);
  }
  for (let i = p.tracks.video.length - 1; i >= 0; i--) {
    if (t >= starts[i]) return located(p, i, t - starts[i]);
  }
  return null;
}

function located(p: Project, clipIndex: number, offsetInClip: number): Located | null {
  const clip = p.tracks.video[clipIndex];
  const media = p.media.find((m) => m.id === clip.mediaId);
  if (!media) return null;
  return { clipIndex, clip, media, offsetInClip };
}

export function overlayWindow(p: Project, o: OverlayItem): { start: number; end: number } | null {
  let start: number;
  if (o.anchor) {
    const idx = p.tracks.video.findIndex((c) => c.id === o.anchor!.clipId);
    if (idx === -1) return null;
    start = clipStartTimes(p)[idx] + o.anchor.offset;
  } else if (o.start !== undefined) {
    start = o.start;
  } else {
    return null;
  }
  const end = o.duration === null ? totalDuration(p) : start + o.duration;
  return { start, end };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/shared`
Expected: PASS（types + timeline 全綠）

- [ ] **Step 5: Commit**

```bash
git add shared/src/timeline.ts shared/src/timeline.test.ts shared/src/index.ts
git commit -m "feat(m1): pure timeline math (totalDuration/locate/overlayWindow)"
```

---

### Task 3: ProjectStore — mutate / patches / history / undo

**Files:**

- Create: `server/package.json`, `server/tsconfig.json`, `server/src/store.ts`
- Test: `server/test/store.test.ts`

**Interfaces:**

- Consumes: `Project`, `createEmptyProject`, `JsonPatch`, `MutationSource`（Task 1）。
- Produces（wsHub、app、ingest、之後 M3 MCP 層全部依賴）：
  ```ts
  class ProjectStore {
    static load(filePath: string): Promise<ProjectStore>; // 檔案不存在 → 從檔名推 name 建空專案
    get doc(): Project; // 唯讀凍結參照
    get version(): number; // 從 0 起算
    mutate(
      source: MutationSource,
      label: string,
      recipe: (draft: Project) => void,
    ): { version: number; patches: JsonPatch[] };
    undo(steps?: number): { version: number } | null; // 無可撤回時回 null
    history(): ReadonlyArray<HistoryEntry>; // HistoryEntry = {version,label,source,ts,patches,inversePatches}
    onChange(
      cb: (e: {
        version: number;
        patches: JsonPatch[];
        source: MutationSource;
        label: string;
      }) => void,
    ): () => void;
    flush(): Promise<void>; // 立刻落盤（測試/關機用）
  }
  ```
- 行為規格：debounce 500ms 原子寫入（tmp+rename）；history ring buffer 上限 200；`undo(n)` 把最後 n 筆的 inversePatches 反序套用、以一筆新 mutation（label `undo`）廣播；no-op recipe（無 patch）不推進 version、不進 history、不觸發 onChange。

- [ ] **Step 1: 建 server workspace**

`server/package.json`：

```json
{
  "name": "@vidcut/server",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@vidcut/shared": "*",
    "express": "^5.0.0",
    "immer": "^10.1.0",
    "nanoid": "^5.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
```

`server/tsconfig.json`：`{ "extends": "../tsconfig.base.json", "include": ["src", "test"] }`

Run: `npm install`（root）
Expected: 安裝成功

- [ ] **Step 2: 寫失敗測試**

`server/test/store.test.ts`：

```ts
import { describe, it, expect, vi } from 'vitest';
import { mkdtemp, readFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { ProjectStore } from '../src/store.js';

async function tmpStore() {
  const dir = await mkdtemp(join(tmpdir(), 'vidcut-'));
  const file = join(dir, 'project.json');
  return { store: await ProjectStore.load(file), file };
}

describe('ProjectStore', () => {
  it('load creates empty project when file missing', async () => {
    const { store } = await tmpStore();
    expect(store.version).toBe(0);
    expect(store.doc.schemaVersion).toBe(1);
  });

  it('mutate bumps version, records history, notifies listeners', async () => {
    const { store } = await tmpStore();
    const seen: string[] = [];
    store.onChange((e) => seen.push(`${e.version}:${e.label}:${e.source}`));
    const r = store.mutate('ai', 'add clip', (d) => {
      d.tracks.video.push({ id: 'c1', mediaId: 'm1', in: 0, duration: 5, volume: 1 });
    });
    expect(r.version).toBe(1);
    expect(r.patches.length).toBeGreaterThan(0);
    expect(store.doc.tracks.video).toHaveLength(1);
    expect(seen).toEqual(['1:add clip:ai']);
    expect(store.history()).toHaveLength(1);
  });

  it('no-op mutate does not bump version or notify', async () => {
    const { store } = await tmpStore();
    const cb = vi.fn();
    store.onChange(cb);
    store.mutate('human', 'noop', () => {});
    expect(store.version).toBe(0);
    expect(cb).not.toHaveBeenCalled();
    expect(store.history()).toHaveLength(0);
  });

  it('undo reverts last mutation as a new version', async () => {
    const { store } = await tmpStore();
    store.mutate('ai', 'add', (d) => {
      d.tracks.video.push({ id: 'c1', mediaId: 'm1', in: 0, duration: 5, volume: 1 });
    });
    store.mutate('human', 'trim', (d) => {
      d.tracks.video[0].duration = 3;
    });
    const r = store.undo(1);
    expect(r?.version).toBe(3);
    expect(store.doc.tracks.video[0].duration).toBe(5);
    expect(store.undo(99)).not.toBeNull(); // 全撤（含撤 undo 自己 = redo 語意，可接受）
  });

  it('flush persists atomically; load round-trips', async () => {
    const { store, file } = await tmpStore();
    store.mutate('ai', 'name', (d) => {
      d.name = 'hello';
    });
    await store.flush();
    const onDisk = JSON.parse(await readFile(file, 'utf8'));
    expect(onDisk.name).toBe('hello');
    const again = await ProjectStore.load(file);
    expect(again.doc.name).toBe('hello');
  });

  it('history is capped at 200 entries', async () => {
    const { store } = await tmpStore();
    for (let i = 0; i < 210; i++)
      store.mutate('ai', `m${i}`, (d) => {
        d.name = `n${i}`;
      });
    expect(store.history()).toHaveLength(200);
    expect(store.history()[0].label).toBe('m10');
  });
});
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `npm run test -w @vidcut/server`
Expected: FAIL（`ProjectStore` 不存在）

- [ ] **Step 4: 實作 `server/src/store.ts`**

```ts
import { enablePatches, produceWithPatches, applyPatches, freeze, type Patch } from 'immer';
import { readFile, writeFile, rename, mkdir } from 'node:fs/promises';
import { dirname, basename, join } from 'node:path';
import {
  createEmptyProject,
  type JsonPatch,
  type MutationSource,
  type Project,
} from '@vidcut/shared';

enablePatches();

export interface HistoryEntry {
  version: number;
  label: string;
  source: MutationSource;
  ts: string;
  patches: JsonPatch[];
  inversePatches: JsonPatch[];
}

export interface ChangeEvent {
  version: number;
  patches: JsonPatch[];
  source: MutationSource;
  label: string;
}

const HISTORY_MAX = 200;
const SAVE_DEBOUNCE_MS = 500;

export class ProjectStore {
  #doc: Project;
  #version = 0;
  #history: HistoryEntry[] = [];
  #listeners = new Set<(e: ChangeEvent) => void>();
  #filePath: string;
  #saveTimer: ReturnType<typeof setTimeout> | null = null;
  #saving: Promise<void> = Promise.resolve();

  private constructor(filePath: string, doc: Project) {
    this.#filePath = filePath;
    this.#doc = freeze(doc, true);
  }

  static async load(filePath: string): Promise<ProjectStore> {
    try {
      const raw = await readFile(filePath, 'utf8');
      return new ProjectStore(filePath, JSON.parse(raw) as Project);
    } catch {
      const name = basename(dirname(filePath)) || 'untitled';
      return new ProjectStore(filePath, createEmptyProject(name, name));
    }
  }

  get doc(): Project {
    return this.#doc;
  }
  get version(): number {
    return this.#version;
  }
  history(): ReadonlyArray<HistoryEntry> {
    return this.#history;
  }

  mutate(
    source: MutationSource,
    label: string,
    recipe: (draft: Project) => void,
  ): { version: number; patches: JsonPatch[] } {
    const [next, patches, inversePatches] = produceWithPatches(this.#doc, recipe);
    if (patches.length === 0) return { version: this.#version, patches: [] };
    this.#doc = next;
    this.#version += 1;
    this.#history.push({
      version: this.#version,
      label,
      source,
      ts: new Date().toISOString(),
      patches: patches as JsonPatch[],
      inversePatches: inversePatches as JsonPatch[],
    });
    if (this.#history.length > HISTORY_MAX)
      this.#history.splice(0, this.#history.length - HISTORY_MAX);
    this.#scheduleSave();
    const evt: ChangeEvent = {
      version: this.#version,
      patches: patches as JsonPatch[],
      source,
      label,
    };
    for (const cb of this.#listeners) cb(evt);
    return { version: this.#version, patches: patches as JsonPatch[] };
  }

  undo(steps = 1): { version: number } | null {
    const n = Math.min(steps, this.#history.length);
    if (n === 0) return null;
    const inverse = this.#history
      .slice(-n)
      .reverse()
      .flatMap((h) => h.inversePatches);
    const r = this.mutate('human', 'undo', (draft) => {
      applyPatches(draft, inverse as Patch[]);
    });
    return { version: r.version };
  }

  onChange(cb: (e: ChangeEvent) => void): () => void {
    this.#listeners.add(cb);
    return () => this.#listeners.delete(cb);
  }

  #scheduleSave(): void {
    if (this.#saveTimer) clearTimeout(this.#saveTimer);
    this.#saveTimer = setTimeout(() => {
      void this.#save();
    }, SAVE_DEBOUNCE_MS);
  }

  async #save(): Promise<void> {
    this.#saving = this.#saving.then(async () => {
      await mkdir(dirname(this.#filePath), { recursive: true });
      const tmp = join(dirname(this.#filePath), `.project.json.tmp`);
      await writeFile(tmp, JSON.stringify(this.#doc, null, 2), 'utf8');
      await rename(tmp, this.#filePath);
    });
    await this.#saving;
  }

  async flush(): Promise<void> {
    if (this.#saveTimer) {
      clearTimeout(this.#saveTimer);
      this.#saveTimer = null;
    }
    await this.#save();
  }
}
```

（注意 `undo` 的語意：撤 undo 自己等於 redo，M1 接受這個簡化——spec §4.2 只要求逐步 undo。）

測試檔裡那行 `new ProjectStore ? ...` 是防手滑寫的贅句——實作時把該行刪掉，改成單純 `expect(store.undo(99)).not.toBeNull();`。

- [ ] **Step 5: 跑測試確認通過**

Run: `npm run test -w @vidcut/server`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add server
git commit -m "feat(m1): ProjectStore with immer patches, history ring, undo, atomic save"
```

---

### Task 4: HTTP app + WebSocket hub

**Files:**

- Create: `server/src/app.ts`, `server/src/wsHub.ts`, `server/src/index.ts`
- Test: `server/test/ws.test.ts`

**Interfaces:**

- Consumes: `ProjectStore`（Task 3）、`WsServerMsg`/`WsClientMsg`（Task 1）。
- Produces:
  - `createApp(store: ProjectStore, projectDir: string, uiDistDir?: string): express.Express` — 路由：`GET /api/project`（debug 用回 `{version, doc}`）、`/media/*` 靜態（`express.static(projectDir)`，原生 Range）、`/`（uiDistDir 存在時服務 UI build）
  - `attachWs(httpServer: http.Server, store: ProjectStore): void` — `/ws` 路徑；連上即發 `full`；store.onChange → 廣播 `patch`；收到 `{type:'resync'}` → 回 `full`
  - `startServer(projectDir: string, port?: number): Promise<{ server: http.Server; store: ProjectStore }>`（index.ts 匯出，測試與 CLI 共用；綁 `127.0.0.1`）
- CLI：`npm run dev -w @vidcut/server -- <projectDir>` 啟動。

- [ ] **Step 1: 寫失敗測試**

`server/test/ws.test.ts`：

```ts
import { describe, it, expect, afterEach } from 'vitest';
import { mkdtemp, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import WebSocket from 'ws';
import type { WsServerMsg } from '@vidcut/shared';
import { startServer } from '../src/index.js';

let close: (() => Promise<void>) | null = null;
afterEach(async () => {
  await close?.();
  close = null;
});

function nextMsg(ws: WebSocket): Promise<WsServerMsg> {
  return new Promise((resolve, reject) => {
    ws.once('message', (d) => resolve(JSON.parse(d.toString())));
    ws.once('error', reject);
  });
}

describe('ws sync', () => {
  it('sends full on connect, then patches on mutate; media served with Range', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'vidcut-ws-'));
    await writeFile(join(dir, 'blob.bin'), Buffer.alloc(100, 7));
    const { server, store } = await startServer(dir, 0); // port 0 = 隨機
    const addr = server.address();
    const port = typeof addr === 'object' && addr ? addr.port : 0;
    close = () => new Promise((r) => server.close(() => r()));

    const ws = new WebSocket(`ws://127.0.0.1:${port}/ws`);
    const full = await nextMsg(ws);
    expect(full.type).toBe('full');
    if (full.type !== 'full') throw new Error('unreachable');
    expect(full.version).toBe(0);

    const patchP = nextMsg(ws);
    store.mutate('ai', 'rename', (d) => {
      d.name = 'x';
    });
    const patch = await patchP;
    expect(patch).toMatchObject({ type: 'patch', version: 1, source: 'ai', label: 'rename' });

    // resync
    const fullP = nextMsg(ws);
    ws.send(JSON.stringify({ type: 'resync' }));
    const full2 = await fullP;
    expect(full2).toMatchObject({ type: 'full', version: 1 });
    ws.close();

    // /media Range
    const res = await fetch(`http://127.0.0.1:${port}/media/blob.bin`, {
      headers: { Range: 'bytes=0-9' },
    });
    expect(res.status).toBe(206);
    expect((await res.arrayBuffer()).byteLength).toBe(10);

    // /api/project
    const api = await fetch(`http://127.0.0.1:${port}/api/project`);
    expect(((await api.json()) as { version: number }).version).toBe(1);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/server`
Expected: FAIL（`startServer` 不存在）

- [ ] **Step 3: 實作**

`server/src/app.ts`：

```ts
import express from 'express';
import { existsSync } from 'node:fs';
import type { ProjectStore } from './store.js';

export function createApp(
  store: ProjectStore,
  projectDir: string,
  uiDistDir?: string,
): express.Express {
  const app = express();
  app.use(express.json());
  app.get('/api/project', (_req, res) => {
    res.json({ version: store.version, doc: store.doc });
  });
  app.use('/media', express.static(projectDir, { fallthrough: false }));
  if (uiDistDir && existsSync(uiDistDir)) app.use(express.static(uiDistDir));
  return app;
}
```

`server/src/wsHub.ts`：

```ts
import { WebSocketServer, WebSocket } from 'ws';
import type { Server } from 'node:http';
import type { WsClientMsg, WsServerMsg } from '@vidcut/shared';
import type { ProjectStore } from './store.js';

export function attachWs(httpServer: Server, store: ProjectStore): void {
  const wss = new WebSocketServer({ server: httpServer, path: '/ws' });

  const send = (ws: WebSocket, msg: WsServerMsg) => {
    if (ws.readyState === WebSocket.OPEN) ws.send(JSON.stringify(msg));
  };
  const full = (): WsServerMsg => ({ type: 'full', version: store.version, doc: store.doc });

  store.onChange((e) => {
    const msg: WsServerMsg = {
      type: 'patch',
      version: e.version,
      patches: e.patches,
      source: e.source,
      label: e.label,
    };
    for (const client of wss.clients) send(client, msg);
  });

  wss.on('connection', (ws) => {
    send(ws, full());
    ws.on('message', (data) => {
      try {
        const msg = JSON.parse(data.toString()) as WsClientMsg;
        if (msg.type === 'resync') send(ws, full());
      } catch {
        /* 非 JSON 訊息一律忽略 */
      }
    });
  });
}
```

`server/src/index.ts`：

```ts
import { createServer, type Server } from 'node:http';
import { join, resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { createApp } from './app.js';
import { attachWs } from './wsHub.js';
import { ProjectStore } from './store.js';

const DEFAULT_PORT = 3845;

export async function startServer(
  projectDir: string,
  port = DEFAULT_PORT,
): Promise<{ server: Server; store: ProjectStore }> {
  const store = await ProjectStore.load(join(projectDir, 'project.json'));
  const uiDist = resolve(dirname(fileURLToPath(import.meta.url)), '../../ui/dist');
  const app = createApp(store, projectDir, uiDist);
  const server = createServer(app);
  attachWs(server, store);
  await new Promise<void>((r) => server.listen(port, '127.0.0.1', r));
  return { server, store };
}

// CLI: tsx src/index.ts <projectDir>
const isMain = process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url);
if (isMain) {
  const dir = process.argv[2];
  if (!dir) {
    console.error('usage: dev -- <projectDir>');
    process.exit(1);
  }
  const { server } = await startServer(resolve(dir));
  const addr = server.address();
  console.log(
    `vidcut server on http://127.0.0.1:${typeof addr === 'object' && addr ? addr.port : DEFAULT_PORT}`,
  );
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/server`
Expected: PASS（store + ws 全綠）

- [ ] **Step 5: Commit**

```bash
git add server/src/app.ts server/src/wsHub.ts server/src/index.ts server/test/ws.test.ts
git commit -m "feat(m1): http app + ws hub with full/patch sync"
```

---

### Task 5: ffmpeg 包裝 — run + probe（含測試 fixture 產生器）

**Files:**

- Create: `server/src/ffmpeg.ts`, `server/test/fixtures.ts`
- Test: `server/test/ffmpeg.test.ts`

**Interfaces:**

- Consumes: `ProbeInfo`（Task 1）。
- Produces（ingest 與 M4 渲染共用）:
  - `runFfmpeg(args: string[]): Promise<void>` — spawn `ffmpeg -hide_banner -y ...args`，非 0 退出丟 Error（訊息含 stderr 最後 2000 字元）
  - `probe(file: string): Promise<ProbeInfo>` — 包 `ffprobe -v error -print_format json -show_streams -show_format`，解析 duration/寬高/fps（`r_frame_rate` 分數）/hasAudio/rotation（`side_data_list` 的 Display Matrix 或 `tags.rotate`；無則 0）
  - 測試 fixture（`server/test/fixtures.ts`）：`makeVideo(dir, name, {duration, withAudio, freq?, color?}): Promise<string>` — lavfi `testsrc2`（540×960@30）+（withAudio 時）`sine`；後續 ingest/整合測試都用它
- 前置：機器需有 ffmpeg（`brew install ffmpeg`）。任務開頭檢查，沒有就先裝。

- [ ] **Step 1: 確認 ffmpeg 存在**

Run: `which ffmpeg ffprobe || brew install ffmpeg`
Expected: 兩個指令都有路徑

- [ ] **Step 2: 寫 fixture 產生器**

`server/test/fixtures.ts`：

```ts
import { join } from 'node:path';
import { runFfmpeg } from '../src/ffmpeg.js';

export interface FixtureOpts {
  duration?: number;
  withAudio?: boolean;
  freq?: number;
  color?: string;
}

/** lavfi 測試影片：直式 540x960@30。withAudio=false 時完全無音軌。 */
export async function makeVideo(
  dir: string,
  name: string,
  opts: FixtureOpts = {},
): Promise<string> {
  const { duration = 4, withAudio = true, freq = 440 } = opts;
  const out = join(dir, name);
  const args = ['-f', 'lavfi', '-i', `testsrc2=duration=${duration}:size=540x960:rate=30`];
  if (withAudio) args.push('-f', 'lavfi', '-i', `sine=frequency=${freq}:duration=${duration}`);
  args.push('-c:v', 'libx264', '-preset', 'ultrafast', '-pix_fmt', 'yuv420p');
  if (withAudio) args.push('-c:a', 'aac', '-shortest');
  args.push(out);
  await runFfmpeg(args);
  return out;
}
```

- [ ] **Step 3: 寫失敗測試**

`server/test/ffmpeg.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { mkdtemp } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { probe, runFfmpeg } from '../src/ffmpeg.js';
import { makeVideo } from './fixtures.js';

describe('ffmpeg wrapper', () => {
  it('probe reads duration/size/fps/audio', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'vidcut-ff-'));
    const f = await makeVideo(dir, 'a.mp4', { duration: 3, withAudio: true });
    const p = await probe(f);
    expect(p.width).toBe(540);
    expect(p.height).toBe(960);
    expect(p.fps).toBeCloseTo(30, 0);
    expect(p.duration).toBeGreaterThan(2.5);
    expect(p.hasAudio).toBe(true);
    expect(p.rotation).toBe(0);
  }, 30_000);

  it('probe detects missing audio', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'vidcut-ff-'));
    const f = await makeVideo(dir, 'mute.mp4', { withAudio: false });
    expect((await probe(f)).hasAudio).toBe(false);
  }, 30_000);

  it('runFfmpeg rejects with stderr on bad args', async () => {
    await expect(runFfmpeg(['-i', '/nonexistent.mp4', '/dev/null/out.mp4'])).rejects.toThrow(
      /nonexistent|No such file/i,
    );
  });
});
```

- [ ] **Step 4: 跑測試確認失敗**

Run: `npm run test -w @vidcut/server`
Expected: FAIL（ffmpeg.ts 不存在——fixtures.ts 也 import 它）

- [ ] **Step 5: 實作 `server/src/ffmpeg.ts`**

```ts
import { spawn } from 'node:child_process';
import type { ProbeInfo } from '@vidcut/shared';

function run(cmd: string, args: string[]): Promise<{ stdout: string; stderr: string }> {
  return new Promise((resolve, reject) => {
    const child = spawn(cmd, args, { stdio: ['ignore', 'pipe', 'pipe'] });
    let stdout = '',
      stderr = '';
    child.stdout.on('data', (d) => {
      stdout += d;
    });
    child.stderr.on('data', (d) => {
      stderr += d;
    });
    child.on('error', reject);
    child.on('close', (code) => {
      if (code === 0) resolve({ stdout, stderr });
      else reject(new Error(`${cmd} exited ${code}: ${stderr.slice(-2000)}`));
    });
  });
}

export async function runFfmpeg(args: string[]): Promise<void> {
  await run('ffmpeg', ['-hide_banner', '-y', ...args]);
}

interface FfprobeStream {
  codec_type: string;
  width?: number;
  height?: number;
  r_frame_rate?: string;
  side_data_list?: Array<{ rotation?: number }>;
  tags?: Record<string, string>;
}

export async function probe(file: string): Promise<ProbeInfo> {
  const { stdout } = await run('ffprobe', [
    '-v',
    'error',
    '-print_format',
    'json',
    '-show_streams',
    '-show_format',
    file,
  ]);
  const data = JSON.parse(stdout) as { streams: FfprobeStream[]; format: { duration?: string } };
  const v = data.streams.find((s) => s.codec_type === 'video');
  if (!v) throw new Error(`no video stream in ${file}`);
  const [num, den] = (v.r_frame_rate ?? '30/1').split('/').map(Number);
  const rotation =
    v.side_data_list?.find((s) => s.rotation !== undefined)?.rotation ??
    Number(v.tags?.rotate ?? 0);
  return {
    duration: Number(data.format.duration ?? 0),
    width: v.width ?? 0,
    height: v.height ?? 0,
    fps: den ? num / den : 30,
    hasAudio: data.streams.some((s) => s.codec_type === 'audio'),
    rotation: Math.abs(rotation) % 360,
  };
}
```

- [ ] **Step 6: 跑測試確認通過**

Run: `npm run test -w @vidcut/server`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add server/src/ffmpeg.ts server/test/fixtures.ts server/test/ffmpeg.test.ts
git commit -m "feat(m1): ffmpeg run/probe wrappers + lavfi test fixtures"
```

---

### Task 6: Ingest 管線 — proxy / filmstrip / peaks / 登記

**Files:**

- Create: `server/src/ingest.ts`
- Test: `server/test/ingest.test.ts`

**Interfaces:**

- Consumes: `runFfmpeg`, `probe`（Task 5）、`ProjectStore`（Task 3）、`MediaAsset`（Task 1）、`nanoid`。
- Produces（M3 的 `import_media` MCP 工具直接呼叫這個）:
  - `ingestMedia(store: ProjectStore, projectDir: string, relPath: string, opts?: { label?: string; meta?: Record<string, unknown> }): Promise<string>` — 回傳 mediaId。
- 行為規格：素材檔已在 `projectDir` 內（`relPath` 相對路徑）；衍生檔寫到 `projectDir/derived/<mediaId>/`（`proxy.mp4`、`filmstrip.jpg`、`peaks.json`）；probe → proxy（Global Constraints 的精確參數；**無音軌時第二輸入 `anullsrc=r=44100:cl=stereo` + `-shortest`**）→ filmstrip（`fps=1,scale=-2:80,tile=Nx1`，N=ceil(duration)，單列 sprite）→ peaks（8kHz mono s16le raw PCM → 每 160 樣本一桶取 max |amplitude| 正規化 0–1，JSON `{samplesPerBucket:160, sampleRate:8000, peaks:number[]}`）→ 以一筆 mutation 把 `MediaAsset` push 進 `doc.media`（含全部衍生檔相對路徑）。重複 ingest 同一 `relPath` 直接回傳既有 mediaId（冪等）。

- [ ] **Step 1: 寫失敗測試**

`server/test/ingest.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { mkdtemp, readFile, stat } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { ProjectStore } from '../src/store.js';
import { ingestMedia } from '../src/ingest.js';
import { probe } from '../src/ffmpeg.js';
import { makeVideo } from './fixtures.js';

async function setup() {
  const dir = await mkdtemp(join(tmpdir(), 'vidcut-ingest-'));
  const store = await ProjectStore.load(join(dir, 'project.json'));
  return { dir, store };
}

describe('ingestMedia', () => {
  it('produces proxy + filmstrip + peaks and registers the asset', async () => {
    const { dir, store } = await setup();
    await makeVideo(dir, 'src.mp4', { duration: 4, withAudio: true });
    const id = await ingestMedia(store, dir, 'src.mp4', { label: 'N1' });

    const asset = store.doc.media.find((m) => m.id === id)!;
    expect(asset.label).toBe('N1');
    expect(asset.probe.hasAudio).toBe(true);

    const proxy = await probe(join(dir, asset.proxyPath!));
    expect(proxy.height).toBe(960);
    expect(proxy.hasAudio).toBe(true);

    expect((await stat(join(dir, asset.filmstripPath!))).size).toBeGreaterThan(0);

    const peaks = JSON.parse(await readFile(join(dir, asset.peaksPath!), 'utf8')) as {
      samplesPerBucket: number;
      sampleRate: number;
      peaks: number[];
    };
    expect(peaks.sampleRate).toBe(8000);
    // 4 秒 × 8000Hz ÷ 160 樣本/桶 ≈ 200 桶
    expect(peaks.peaks.length).toBeGreaterThan(150);
    expect(Math.max(...peaks.peaks)).toBeLessThanOrEqual(1);
  }, 60_000);

  it('injects silent audio track for mute sources', async () => {
    const { dir, store } = await setup();
    await makeVideo(dir, 'mute.mp4', { withAudio: false });
    const id = await ingestMedia(store, dir, 'mute.mp4');
    const asset = store.doc.media.find((m) => m.id === id)!;
    expect(asset.probe.hasAudio).toBe(false); // 原始檔的事實
    expect((await probe(join(dir, asset.proxyPath!))).hasAudio).toBe(true); // proxy 一定有音軌
  }, 60_000);

  it('is idempotent per relPath', async () => {
    const { dir, store } = await setup();
    await makeVideo(dir, 'src.mp4', {});
    const a = await ingestMedia(store, dir, 'src.mp4');
    const b = await ingestMedia(store, dir, 'src.mp4');
    expect(b).toBe(a);
    expect(store.doc.media).toHaveLength(1);
  }, 60_000);
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/server`
Expected: FAIL（ingest.ts 不存在）

- [ ] **Step 3: 實作 `server/src/ingest.ts`**

```ts
import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import { tmpdir } from 'node:os';
import { mkdtemp } from 'node:fs/promises';
import { nanoid } from 'nanoid';
import type { MediaAsset } from '@vidcut/shared';
import { probe, runFfmpeg } from './ffmpeg.js';
import type { ProjectStore } from './store.js';

export interface IngestOpts {
  label?: string;
  meta?: Record<string, unknown>;
}

export async function ingestMedia(
  store: ProjectStore,
  projectDir: string,
  relPath: string,
  opts: IngestOpts = {},
): Promise<string> {
  const existing = store.doc.media.find((m) => m.path === relPath);
  if (existing) return existing.id;

  const abs = join(projectDir, relPath);
  const info = await probe(abs);
  const id = nanoid(8);
  const derivedRel = join('derived', id);
  const derivedAbs = join(projectDir, derivedRel);
  await mkdir(derivedAbs, { recursive: true });

  // 1. proxy —— spec §8.1 精確參數；無音軌補 anullsrc
  const proxyArgs = ['-i', abs];
  if (!info.hasAudio) proxyArgs.push('-f', 'lavfi', '-i', 'anullsrc=r=44100:cl=stereo');
  proxyArgs.push(
    '-vf',
    'scale=-2:960:flags=bicubic,fps=30',
    '-c:v',
    'libx264',
    '-profile:v',
    'high',
    '-preset',
    'veryfast',
    '-crf',
    '23',
    '-g',
    '15',
    '-keyint_min',
    '15',
    '-sc_threshold',
    '0',
    '-tune',
    'fastdecode',
    '-pix_fmt',
    'yuv420p',
    '-c:a',
    'aac',
    '-b:a',
    '128k',
    '-ac',
    '2',
  );
  if (!info.hasAudio) proxyArgs.push('-shortest');
  proxyArgs.push('-movflags', '+faststart', join(derivedAbs, 'proxy.mp4'));
  await runFfmpeg(proxyArgs);

  // 2. filmstrip —— 每秒 1 幀單列 sprite
  const frames = Math.max(1, Math.ceil(info.duration));
  await runFfmpeg([
    '-i',
    abs,
    '-vf',
    `fps=1,scale=-2:80,tile=${frames}x1`,
    '-frames:v',
    '1',
    '-q:v',
    '3',
    join(derivedAbs, 'filmstrip.jpg'),
  ]);

  // 3. peaks —— 8kHz mono s16le → 160 樣本/桶 max|amp| 正規化
  const pcmDir = await mkdtemp(join(tmpdir(), 'vidcut-pcm-'));
  const pcmFile = join(pcmDir, 'a.pcm');
  const pcmSrc = info.hasAudio ? abs : join(derivedAbs, 'proxy.mp4'); // 無音軌用 proxy 的靜音軌
  await runFfmpeg(['-i', pcmSrc, '-ac', '1', '-ar', '8000', '-f', 's16le', pcmFile]);
  const pcm = await readFile(pcmFile);
  const samplesPerBucket = 160;
  const peaks: number[] = [];
  for (let i = 0; i + 1 < pcm.length; i += samplesPerBucket * 2) {
    let max = 0;
    for (let j = i; j < Math.min(i + samplesPerBucket * 2, pcm.length - 1); j += 2) {
      max = Math.max(max, Math.abs(pcm.readInt16LE(j)));
    }
    peaks.push(Number((max / 32768).toFixed(4)));
  }
  await writeFile(
    join(derivedAbs, 'peaks.json'),
    JSON.stringify({ samplesPerBucket, sampleRate: 8000, peaks }),
  );

  // 4. 登記（單一 mutation）
  const asset: MediaAsset = {
    id,
    path: relPath,
    proxyPath: join(derivedRel, 'proxy.mp4'),
    filmstripPath: join(derivedRel, 'filmstrip.jpg'),
    peaksPath: join(derivedRel, 'peaks.json'),
    probe: info,
    ...(opts.label ? { label: opts.label } : {}),
    ...(opts.meta ? { meta: opts.meta } : {}),
  };
  store.mutate('ai', `import ${relPath}`, (d) => {
    d.media.push(asset);
  });
  return id;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/server`
Expected: PASS（三個 ingest 測試全綠；lavfi 轉檔較慢屬正常）

- [ ] **Step 5: Commit**

```bash
git add server/src/ingest.ts server/test/ingest.test.ts
git commit -m "feat(m1): ingest pipeline (proxy/filmstrip/peaks) with silent-audio injection"
```

---

### Task 7: UI 骨架 + WS client + project store

**Files:**

- Create: `ui/package.json`, `ui/tsconfig.json`, `ui/vite.config.ts`, `ui/index.html`, `ui/src/main.tsx`, `ui/src/App.tsx`, `ui/src/ws.ts`, `ui/src/stores/project.ts`
- Test: `ui/src/stores/project.test.ts`

**Interfaces:**

- Consumes: `Project`, `WsServerMsg`, `JsonPatch`（Task 1）。
- Produces:
  - zustand store `useProject`：state `{ doc: Project | null; version: number; connected: boolean }`；actions `applyServerMsg(msg: WsServerMsg)`, `setConnected(b: boolean)`
  - `connectWs(url: string): void` — 原生 WebSocket、斷線指數退避（1s 起、上限 10s）重連、重連成功即發 `{type:'resync'}`
  - `mediaUrl(m: { proxyPath?: string; path: string }): string` — `/media/` + (proxyPath ?? path)（`ui/src/ws.ts` 一併匯出）
- Vite dev proxy：`/ws`（ws:true）、`/media`、`/api` → `http://127.0.0.1:3845`。

- [ ] **Step 1: 建 UI workspace**

`ui/package.json`：

```json
{
  "name": "@vidcut/ui",
  "version": "0.0.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "test": "vitest run" },
  "dependencies": {
    "@vidcut/shared": "*",
    "immer": "^10.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
```

`ui/tsconfig.json`：

```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true
  },
  "include": ["src"]
}
```

`ui/vite.config.ts`：

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/ws': { target: 'ws://127.0.0.1:3845', ws: true },
      '/media': 'http://127.0.0.1:3845',
      '/api': 'http://127.0.0.1:3845',
    },
  },
});
```

`ui/index.html`：

```html
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <title>vidcut</title>
    <style>
      body {
        margin: 0;
        background: #111;
        color: #eee;
        font-family: system-ui;
      }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Run: `npm install`
Expected: 成功

- [ ] **Step 2: 寫失敗測試（store 的 patch 套用）**

`ui/src/stores/project.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { createEmptyProject } from '@vidcut/shared';
import { useProject } from './project.js';

describe('useProject store', () => {
  beforeEach(() => useProject.setState({ doc: null, version: 0, connected: false }));

  it('full replaces doc', () => {
    const doc = createEmptyProject('p', 'demo');
    useProject.getState().applyServerMsg({ type: 'full', version: 3, doc });
    expect(useProject.getState().doc?.name).toBe('demo');
    expect(useProject.getState().version).toBe(3);
  });

  it('patch applies incrementally when version is consecutive', () => {
    const doc = createEmptyProject('p', 'demo');
    useProject.getState().applyServerMsg({ type: 'full', version: 1, doc });
    useProject.getState().applyServerMsg({
      type: 'patch',
      version: 2,
      source: 'ai',
      label: 'rename',
      patches: [{ op: 'replace', path: ['name'], value: 'renamed' }],
    });
    expect(useProject.getState().doc?.name).toBe('renamed');
    expect(useProject.getState().version).toBe(2);
  });

  it('gapped patch version triggers resync request instead of applying', () => {
    const doc = createEmptyProject('p', 'demo');
    useProject.getState().applyServerMsg({ type: 'full', version: 1, doc });
    const wanted = useProject.getState().applyServerMsg({
      type: 'patch',
      version: 5,
      source: 'ai',
      label: 'x',
      patches: [{ op: 'replace', path: ['name'], value: 'skip' }],
    });
    expect(wanted).toBe('resync'); // caller（ws.ts）負責真的發 resync
    expect(useProject.getState().doc?.name).toBe('demo'); // 未套用
  });
});
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `npm run test -w @vidcut/ui`
Expected: FAIL（store 不存在）

- [ ] **Step 4: 實作**

`ui/src/stores/project.ts`：

```ts
import { create } from 'zustand';
import { applyPatches, enablePatches, type Patch } from 'immer';
import type { Project, WsServerMsg } from '@vidcut/shared';

enablePatches();

interface ProjectState {
  doc: Project | null;
  version: number;
  connected: boolean;
  setConnected: (b: boolean) => void;
  /** 回 'resync' 表示 patch 版本跳號，呼叫端需要重新同步 */
  applyServerMsg: (msg: WsServerMsg) => 'ok' | 'resync';
}

export const useProject = create<ProjectState>((set, get) => ({
  doc: null,
  version: 0,
  connected: false,
  setConnected: (b) => set({ connected: b }),
  applyServerMsg: (msg) => {
    if (msg.type === 'full') {
      set({ doc: msg.doc, version: msg.version });
      return 'ok';
    }
    const { doc, version } = get();
    if (!doc || msg.version !== version + 1) return 'resync';
    set({ doc: applyPatches(doc, msg.patches as Patch[]), version: msg.version });
    return 'ok';
  },
}));
```

`ui/src/ws.ts`：

```ts
import type { WsServerMsg } from '@vidcut/shared';
import { useProject } from './stores/project.js';

export function mediaUrl(m: { proxyPath?: string; path: string }): string {
  return `/media/${m.proxyPath ?? m.path}`;
}

export function connectWs(url = `ws://${location.host}/ws`): void {
  let delay = 1000;
  const open = () => {
    const ws = new WebSocket(url);
    ws.onopen = () => {
      delay = 1000;
      useProject.getState().setConnected(true);
      ws.send(JSON.stringify({ type: 'resync' }));
    };
    ws.onmessage = (ev) => {
      const msg = JSON.parse(ev.data as string) as WsServerMsg;
      if (useProject.getState().applyServerMsg(msg) === 'resync') {
        ws.send(JSON.stringify({ type: 'resync' }));
      }
    };
    ws.onclose = () => {
      useProject.getState().setConnected(false);
      setTimeout(open, delay);
      delay = Math.min(delay * 2, 10_000);
    };
    ws.onerror = () => ws.close();
  };
  open();
}
```

`ui/src/main.tsx`：

```tsx
import { createRoot } from 'react-dom/client';
import { App } from './App.js';
import { connectWs } from './ws.js';

connectWs();
createRoot(document.getElementById('root')!).render(<App />);
```

`ui/src/App.tsx`（暫時版，Task 10/11 會替換內容）：

```tsx
import { useProject } from './stores/project.js';

export function App() {
  const { doc, version, connected } = useProject();
  return (
    <div style={{ padding: 16 }}>
      <h1>vidcut</h1>
      <p>
        {connected ? '🟢 已連線' : '🔴 連線中…'}　專案：{doc?.name ?? '—'}　v{version}
      </p>
    </div>
  );
}
```

- [ ] **Step 5: 跑測試確認通過**

Run: `npm run test -w @vidcut/ui`
Expected: PASS

- [ ] **Step 6: 手動驗證端到端同步**

```bash
mkdir -p projects/demo
npm run dev:server -- projects/demo   # 終端機 A
npm run dev:ui                        # 終端機 B → http://localhost:5173
```

瀏覽器應顯示「🟢 已連線 專案：demo v0」。停掉 server 應轉紅、重啟後自動轉綠（重連 + resync）。

- [ ] **Step 7: Commit**

```bash
git add ui
git commit -m "feat(m1): react ui scaffold with ws sync + patch-applying store"
```

---

### Task 8: 播放計畫純函數（`ui/src/player/plan.ts`）

**Files:**

- Create: `ui/src/player/plan.ts`
- Test: `ui/src/player/plan.test.ts`

**Interfaces:**

- Consumes: `locate`, `clipStartTimes`, `overlayWindow`, `totalDuration`（Task 2）、`mediaUrl`（Task 7）。
- Produces（Player 組件的唯一決策來源）:

  ```ts
  interface ActiveSource {
    clipIndex: number;
    clipId: string;
    src: string;
    sourceTime: number;
  } // sourceTime = clip.in + offset
  interface PlayerPlan {
    active: ActiveSource | null;
    next: ActiveSource | null; // 下一 clip 的起點（premount 用；最後一段時 null）
    overlays: Array<{ id: string; src: string; position: { x: number; y: number; scale: number } }>;
    captions: Array<{ id: string; text: string; style: CaptionItem['style'] }>;
    done: boolean; // t 已達片尾
  }
  function planAt(p: Project, t: number): PlayerPlan;
  ```

- [ ] **Step 1: 寫失敗測試**

`ui/src/player/plan.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { createEmptyProject, type Project } from '@vidcut/shared';
import { planAt } from './plan.js';

function proj(): Project {
  const p = createEmptyProject('p', 't');
  p.media = [
    {
      id: 'm1',
      path: 'a.mp4',
      proxyPath: 'derived/m1/proxy.mp4',
      probe: { duration: 20, width: 540, height: 960, fps: 30, hasAudio: true, rotation: 0 },
    },
    {
      id: 'm2',
      path: 'b.mp4',
      proxyPath: 'derived/m2/proxy.mp4',
      probe: { duration: 20, width: 540, height: 960, fps: 30, hasAudio: true, rotation: 0 },
    },
  ];
  p.tracks.video = [
    { id: 'c1', mediaId: 'm1', in: 2, duration: 6, volume: 1 },
    { id: 'c2', mediaId: 'm2', in: 0, duration: 4, volume: 1 },
  ];
  p.tracks.overlays = [
    {
      id: 'o1',
      imagePath: 'ov.png',
      start: 0,
      duration: null,
      position: { x: 0.5, y: 0.1, scale: 1 },
    },
  ];
  p.tracks.captions = [
    {
      id: 'cap1',
      text: 'hi',
      start: 5,
      duration: 3,
      style: { fontFamily: 'sans-serif', fontSize: 48, fill: '#fff', y: 0.8 },
    },
  ];
  return p;
}

describe('planAt', () => {
  it('maps timeline t to active proxy src + source time', () => {
    const plan = planAt(proj(), 3);
    expect(plan.active).toMatchObject({
      clipId: 'c1',
      src: '/media/derived/m1/proxy.mp4',
      sourceTime: 5,
    }); // in 2 + offset 3
    expect(plan.next).toMatchObject({ clipId: 'c2', sourceTime: 0 });
    expect(plan.done).toBe(false);
  });

  it('last clip has no next; end of timeline is done', () => {
    expect(planAt(proj(), 7).next).toBeNull();
    expect(planAt(proj(), 10).done).toBe(true);
  });

  it('overlays/captions filtered by time window', () => {
    expect(planAt(proj(), 1).overlays).toHaveLength(1);
    expect(planAt(proj(), 1).captions).toHaveLength(0);
    expect(planAt(proj(), 6).captions).toMatchObject([{ id: 'cap1', text: 'hi' }]);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/ui`
Expected: FAIL

- [ ] **Step 3: 實作 `ui/src/player/plan.ts`**

```ts
import {
  clipStartTimes,
  locate,
  overlayWindow,
  totalDuration,
  type CaptionItem,
  type Project,
} from '@vidcut/shared';
import { mediaUrl } from '../ws.js';

export interface ActiveSource {
  clipIndex: number;
  clipId: string;
  src: string;
  sourceTime: number;
}

export interface PlayerPlan {
  active: ActiveSource | null;
  next: ActiveSource | null;
  overlays: Array<{ id: string; src: string; position: { x: number; y: number; scale: number } }>;
  captions: Array<{ id: string; text: string; style: CaptionItem['style'] }>;
  done: boolean;
}

function sourceFor(p: Project, clipIndex: number, offsetInClip: number): ActiveSource | null {
  const clip = p.tracks.video[clipIndex];
  if (!clip) return null;
  const media = p.media.find((m) => m.id === clip.mediaId);
  if (!media) return null;
  return { clipIndex, clipId: clip.id, src: mediaUrl(media), sourceTime: clip.in + offsetInClip };
}

export function planAt(p: Project, t: number): PlayerPlan {
  const total = totalDuration(p);
  const done = total > 0 && t >= total;
  const loc = locate(p, Math.min(t, total));
  const active = loc ? sourceFor(p, loc.clipIndex, loc.offsetInClip) : null;
  const next = loc ? sourceFor(p, loc.clipIndex + 1, 0) : null;
  const overlays = p.tracks.overlays
    .filter((o) => {
      const w = overlayWindow(p, o);
      return w && t >= w.start && t < w.end;
    })
    .map((o) => ({ id: o.id, src: `/media/${o.imagePath}`, position: o.position }));
  const captions = p.tracks.captions
    .filter((c) => t >= c.start && t < c.start + c.duration)
    .map((c) => ({ id: c.id, text: c.text, style: c.style }));
  return { active, next, overlays, captions, done };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/ui`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add ui/src/player
git commit -m "feat(m1): pure playback planner (planAt)"
```

---

### Task 9: playback store + Player 組件（A/B 無縫引擎）

**Files:**

- Create: `ui/src/stores/playback.ts`, `ui/src/player/Player.tsx`
- Modify: `ui/src/App.tsx`（掛上 Player）
- Test: `ui/src/stores/playback.test.ts`（store 純邏輯）；Player 本體為手動驗證（Task 12）

**Interfaces:**

- Consumes: `planAt`（Task 8）、`useProject`（Task 7）、`totalDuration`（Task 2）。
- Produces:
  - zustand store `usePlayback`：state `{ time: number; playing: boolean }`；actions `play()`, `pause()`, `seek(t: number)`（clamp 到 `[0, total]`，Timeline 也會用）
  - `<Player />` 組件：9:16 畫布（max-height 70vh）、A/B `<video muted playsInline>`、overlay img 層、caption 層、播放/暫停鈕、時間顯示
- 引擎規格（spec §7）：rAF 主時鐘（`performance.now()` 差分推進 `time`）；非 active 的 video 元素 premount 下一片段並 pre-seek；邊界前 50ms 對 next `play()`（保持 muted）到點交換可見性；active video 與期望 sourceTime 漂移 > 60ms 時校正 `currentTime`；`done` 時自動 pause 並停在片尾；音訊直接用 active video 的聲音（`muted=false` 只在 active）。

- [ ] **Step 1: 寫失敗測試（playback store）**

`ui/src/stores/playback.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { usePlayback } from './playback.js';

describe('usePlayback', () => {
  beforeEach(() => usePlayback.setState({ time: 0, playing: false, total: 10 }));

  it('play/pause toggles', () => {
    usePlayback.getState().play();
    expect(usePlayback.getState().playing).toBe(true);
    usePlayback.getState().pause();
    expect(usePlayback.getState().playing).toBe(false);
  });

  it('seek clamps into [0, total]', () => {
    usePlayback.getState().seek(-5);
    expect(usePlayback.getState().time).toBe(0);
    usePlayback.getState().seek(99);
    expect(usePlayback.getState().time).toBe(10);
    usePlayback.getState().seek(4.2);
    expect(usePlayback.getState().time).toBe(4.2);
  });

  it('tick advances time and auto-pauses at end', () => {
    usePlayback.setState({ playing: true });
    usePlayback.getState().tick(9.5, 1.0); // from, dt
    expect(usePlayback.getState().time).toBe(10);
    expect(usePlayback.getState().playing).toBe(false);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/ui`
Expected: FAIL

- [ ] **Step 3: 實作 `ui/src/stores/playback.ts`**

```ts
import { create } from 'zustand';

interface PlaybackState {
  time: number;
  playing: boolean;
  total: number; // Player 依 doc 更新
  setTotal: (t: number) => void;
  play: () => void;
  pause: () => void;
  seek: (t: number) => void;
  /** 主時鐘推進：from + dt，撞到片尾自動暫停 */
  tick: (from: number, dt: number) => void;
}

export const usePlayback = create<PlaybackState>((set, get) => ({
  time: 0,
  playing: false,
  total: 0,
  setTotal: (total) => set({ total }),
  play: () => set({ playing: true }),
  pause: () => set({ playing: false }),
  seek: (t) => set({ time: Math.min(Math.max(t, 0), get().total) }),
  tick: (from, dt) => {
    const t = from + dt;
    if (t >= get().total) set({ time: get().total, playing: false });
    else set({ time: t });
  },
}));
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/ui`
Expected: PASS

- [ ] **Step 5: 實作 `ui/src/player/Player.tsx`**

```tsx
import { useEffect, useRef } from 'react';
import { totalDuration } from '@vidcut/shared';
import { useProject } from '../stores/project.js';
import { usePlayback } from '../stores/playback.js';
import { planAt } from './plan.js';

const DRIFT_TOLERANCE = 0.06; // 60ms
const PRELAUNCH = 0.05; // 邊界前 50ms 啟動 next

export function Player() {
  const doc = useProject((s) => s.doc);
  const { time, playing } = usePlayback();
  const vA = useRef<HTMLVideoElement>(null);
  const vB = useRef<HTMLVideoElement>(null);
  const activeIsA = useRef(true);
  const mountedClip = useRef<{ a: string | null; b: string | null }>({ a: null, b: null });

  useEffect(() => {
    if (doc) usePlayback.getState().setTotal(totalDuration(doc));
  }, [doc]);

  // 主時鐘：rAF + performance.now() 差分
  useEffect(() => {
    if (!playing) return;
    let raf = 0;
    let last = performance.now();
    const loop = (now: number) => {
      usePlayback.getState().tick(usePlayback.getState().time, (now - last) / 1000);
      last = now;
      raf = requestAnimationFrame(loop);
    };
    raf = requestAnimationFrame(loop);
    return () => cancelAnimationFrame(raf);
  }, [playing]);

  // 每次 time/doc 變化：對齊 video 元素
  useEffect(() => {
    if (!doc) return;
    const plan = planAt(doc, time);
    const [act, spare] = activeIsA.current ? [vA.current, vB.current] : [vB.current, vA.current];
    const key = activeIsA.current ? 'a' : 'b';
    const spareKey = activeIsA.current ? 'b' : 'a';
    if (!act || !spare) return;

    if (!plan.active) {
      act.pause();
      spare.pause();
      return;
    }

    // 換 clip：spare 已 premount 同一 clip → 交換；否則硬切
    if (mountedClip.current[key] !== plan.active.clipId) {
      if (mountedClip.current[spareKey] === plan.active.clipId) {
        activeIsA.current = !activeIsA.current;
        act.pause();
        act.muted = true;
        spare.muted = false;
        if (playing) void spare.play().catch(() => {});
        return; // 下一輪 effect 以新 active 繼續
      }
      act.src = plan.active.src;
      act.currentTime = plan.active.sourceTime;
      mountedClip.current[key] = plan.active.clipId;
    }

    // 漂移校正
    if (Math.abs(act.currentTime - plan.active.sourceTime) > DRIFT_TOLERANCE) {
      act.currentTime = plan.active.sourceTime;
    }
    act.muted = false;
    if (playing && act.paused) void act.play().catch(() => {});
    if (!playing && !act.paused) act.pause();

    // premount + 預啟動 next
    if (plan.next && mountedClip.current[spareKey] !== plan.next.clipId) {
      spare.src = plan.next.src;
      spare.currentTime = plan.next.sourceTime;
      spare.muted = true;
      mountedClip.current[spareKey] = plan.next.clipId;
    }
    if (plan.next && playing) {
      const clipEnd = doc.tracks.video
        .slice(0, plan.active.clipIndex + 1)
        .reduce((s, c) => s + c.duration, 0);
      if (clipEnd - time < PRELAUNCH && spare.paused) void spare.play().catch(() => {});
    }
  }, [doc, time, playing]);

  if (!doc) return null;
  const plan = planAt(doc, time);
  const vidStyle = (visible: boolean): React.CSSProperties => ({
    position: 'absolute',
    inset: 0,
    width: '100%',
    height: '100%',
    objectFit: 'contain',
    opacity: visible ? 1 : 0,
  });
  return (
    <div>
      <div
        style={{
          position: 'relative',
          aspectRatio: '9/16',
          maxHeight: '70vh',
          margin: '0 auto',
          background: '#000',
        }}
      >
        <video ref={vA} muted playsInline style={vidStyle(activeIsA.current)} />
        <video ref={vB} muted playsInline style={vidStyle(!activeIsA.current)} />
        {plan.overlays.map((o) => (
          <img
            key={o.id}
            src={o.src}
            alt=""
            style={{
              position: 'absolute',
              left: `${o.position.x * 100}%`,
              top: `${o.position.y * 100}%`,
              transform: `translate(-50%, 0) scale(${o.position.scale})`,
              maxWidth: '90%',
              pointerEvents: 'none',
            }}
          />
        ))}
        {plan.captions.map((c) => (
          <div
            key={c.id}
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: `${c.style.y * 100}%`,
              textAlign: 'center',
              fontFamily: c.style.fontFamily,
              fontSize: c.style.fontSize / 3,
              color: c.style.fill,
              WebkitTextStroke: c.style.stroke ? `1px ${c.style.stroke}` : undefined,
              pointerEvents: 'none',
            }}
          >
            {c.text}
          </div>
        ))}
      </div>
      <div style={{ display: 'flex', gap: 8, justifyContent: 'center', padding: 8 }}>
        <button
          onClick={() => (playing ? usePlayback.getState().pause() : usePlayback.getState().play())}
        >
          {playing ? '⏸' : '▶'}
        </button>
        <span>{time.toFixed(2)}s</span>
      </div>
    </div>
  );
}
```

（字幕字級 `/3` 是 1080 畫布→預覽像素的粗略縮放，M2 改為依容器實寬換算。）

`ui/src/App.tsx` 改為：

```tsx
import { useProject } from './stores/project.js';
import { Player } from './player/Player.js';

export function App() {
  const { doc, version, connected } = useProject();
  return (
    <div style={{ padding: 16 }}>
      <p>
        {connected ? '🟢' : '🔴'} {doc?.name ?? '—'} v{version}
      </p>
      <Player />
    </div>
  );
}
```

- [ ] **Step 6: 跑全部 UI 測試 + type check**

Run: `npm run test -w @vidcut/ui && npx tsc --noEmit -p ui`
Expected: PASS / 無型別錯誤

- [ ] **Step 7: Commit**

```bash
git add ui/src
git commit -m "feat(m1): playback store + A/B seamless player with overlay/caption layers"
```

---

### Task 10: 時間軸唯讀組件

**Files:**

- Create: `ui/src/timeline/scale.ts`, `ui/src/timeline/Timeline.tsx`
- Modify: `ui/src/App.tsx`
- Test: `ui/src/timeline/scale.test.ts`

**Interfaces:**

- Consumes: `useProject`、`usePlayback`、`clipStartTimes`、`totalDuration`、`overlayWindow`、peaks JSON 格式（Task 6）。
- Produces:
  - `ui/src/timeline/scale.ts`：`PX_PER_SECOND = 60`（M1 固定）、`timeToPx(t: number): number`、`pxToTime(px: number): number`
  - `<Timeline />`：軌道列（video / overlays / captions / audio）、clip 方塊（filmstrip 為 `background-image`、依 clip.in 偏移 `background-position`）、clip 內波形 canvas（讀 peaksPath JSON、切出 `[in, in+duration]` 區段）、秒刻度尺規、紅色 playhead（跟 `usePlayback.time`）、點擊軌道區 seek

- [ ] **Step 1: 寫失敗測試**

`ui/src/timeline/scale.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { timeToPx, pxToTime, PX_PER_SECOND } from './scale.js';

describe('timeline scale', () => {
  it('round-trips', () => {
    expect(timeToPx(2.5)).toBe(2.5 * PX_PER_SECOND);
    expect(pxToTime(timeToPx(7.3))).toBeCloseTo(7.3);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/ui`
Expected: FAIL

- [ ] **Step 3: 實作**

`ui/src/timeline/scale.ts`：

```ts
export const PX_PER_SECOND = 60; // M1 固定；M2 變成可縮放 state
export const timeToPx = (t: number): number => t * PX_PER_SECOND;
export const pxToTime = (px: number): number => px / PX_PER_SECOND;
```

`ui/src/timeline/Timeline.tsx`：

```tsx
import { useEffect, useRef, useState } from 'react';
import {
  clipStartTimes,
  overlayWindow,
  totalDuration,
  type MediaAsset,
  type Project,
  type VideoClip,
} from '@vidcut/shared';
import { useProject } from '../stores/project.js';
import { usePlayback } from '../stores/playback.js';
import { PX_PER_SECOND, pxToTime, timeToPx } from './scale.js';

const ROW_H = 56;

interface Peaks {
  samplesPerBucket: number;
  sampleRate: number;
  peaks: number[];
}
const peaksCache = new Map<string, Peaks>();

function ClipBlock({ p, clip, start }: { p: Project; clip: VideoClip; start: number }) {
  const media = p.media.find((m) => m.id === clip.mediaId);
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const [peaks, setPeaks] = useState<Peaks | null>(null);
  const w = timeToPx(clip.duration);

  useEffect(() => {
    if (!media?.peaksPath) return;
    const url = `/media/${media.peaksPath}`;
    if (peaksCache.has(url)) {
      setPeaks(peaksCache.get(url)!);
      return;
    }
    void fetch(url)
      .then((r) => r.json())
      .then((j: Peaks) => {
        peaksCache.set(url, j);
        setPeaks(j);
      });
  }, [media?.peaksPath]);

  useEffect(() => {
    // 畫 [in, in+duration] 的波形切片
    const cv = canvasRef.current;
    if (!cv || !peaks) return;
    const ctx = cv.getContext('2d')!;
    ctx.clearRect(0, 0, cv.width, cv.height);
    ctx.fillStyle = 'rgba(120,200,120,0.7)';
    const bucketsPerSec = peaks.sampleRate / peaks.samplesPerBucket;
    const from = Math.floor(clip.in * bucketsPerSec);
    const count = Math.max(1, Math.floor(clip.duration * bucketsPerSec));
    for (let x = 0; x < cv.width; x++) {
      const v = peaks.peaks[from + Math.floor((x / cv.width) * count)] ?? 0;
      const h = v * cv.height;
      ctx.fillRect(x, cv.height - h, 1, h);
    }
  }, [peaks, clip.in, clip.duration]);

  // filmstrip：每秒 1 幀，frame 高 80px；以 background-position 對齊 clip.in
  const filmstrip = media?.filmstripPath ? `/media/${media.filmstripPath}` : undefined;
  const frameW = media ? (80 * media.probe.width) / media.probe.height : 45;
  const bgOffset = -(clip.in * frameW); // 每秒一幀 → in 秒 ≈ in 幀
  return (
    <div
      title={`${clip.label ?? clip.id}  in=${clip.in}s dur=${clip.duration}s`}
      style={{
        position: 'absolute',
        left: timeToPx(start),
        width: w,
        height: ROW_H,
        border: '1px solid #555',
        borderRadius: 4,
        overflow: 'hidden',
        backgroundImage: filmstrip ? `url(${filmstrip})` : undefined,
        backgroundPosition: `${bgOffset}px 0`,
        backgroundSize: 'auto 100%',
        backgroundRepeat: 'repeat-x',
        backgroundColor: '#333',
      }}
    >
      <span
        style={{ position: 'absolute', top: 2, left: 4, fontSize: 11, textShadow: '0 0 3px #000' }}
      >
        {clip.label ?? clip.id}
      </span>
      <canvas
        ref={canvasRef}
        width={Math.max(1, Math.floor(w))}
        height={16}
        style={{ position: 'absolute', bottom: 0, left: 0, width: '100%', height: 16 }}
      />
    </div>
  );
}

export function Timeline() {
  const doc = useProject((s) => s.doc);
  const time = usePlayback((s) => s.time);
  if (!doc) return null;
  const total = totalDuration(doc);
  const starts = clipStartTimes(doc);
  const width = Math.max(timeToPx(total) + 120, 600);

  const onSeek = (e: React.MouseEvent<HTMLDivElement>) => {
    const rect = e.currentTarget.getBoundingClientRect();
    usePlayback.getState().seek(pxToTime(e.clientX - rect.left + e.currentTarget.scrollLeft));
  };

  const rowStyle: React.CSSProperties = {
    position: 'relative',
    height: ROW_H,
    borderBottom: '1px solid #222',
  };
  return (
    <div style={{ overflowX: 'auto', border: '1px solid #333', userSelect: 'none' }}>
      <div style={{ position: 'relative', width }} onClick={onSeek}>
        {/* 尺規 */}
        <div style={{ position: 'relative', height: 20, borderBottom: '1px solid #444' }}>
          {Array.from({ length: Math.ceil(total) + 1 }, (_, s) => (
            <span
              key={s}
              style={{ position: 'absolute', left: timeToPx(s), fontSize: 10, color: '#888' }}
            >
              {s}s
            </span>
          ))}
        </div>
        {/* video 主軌 */}
        <div style={rowStyle}>
          {doc.tracks.video.map((c, i) => (
            <ClipBlock key={c.id} p={doc} clip={c} start={starts[i]} />
          ))}
        </div>
        {/* overlays 軌 */}
        <div style={{ ...rowStyle, height: 24 }}>
          {doc.tracks.overlays.map((o) => {
            const w = overlayWindow(doc, o);
            return (
              w && (
                <div
                  key={o.id}
                  style={{
                    position: 'absolute',
                    left: timeToPx(w.start),
                    width: timeToPx(w.end - w.start),
                    height: 20,
                    background: '#5a4',
                    borderRadius: 3,
                    fontSize: 10,
                    paddingLeft: 4,
                  }}
                >
                  {o.imagePath.split('/').pop()}
                </div>
              )
            );
          })}
        </div>
        {/* captions 軌 */}
        <div style={{ ...rowStyle, height: 24 }}>
          {doc.tracks.captions.map((c) => (
            <div
              key={c.id}
              style={{
                position: 'absolute',
                left: timeToPx(c.start),
                width: timeToPx(c.duration),
                height: 20,
                background: '#46a',
                borderRadius: 3,
                fontSize: 10,
                paddingLeft: 4,
              }}
            >
              {c.text}
            </div>
          ))}
        </div>
        {/* audio 軌 */}
        <div style={{ ...rowStyle, height: 24 }}>
          {doc.tracks.audio.map((a) => (
            <div
              key={a.id}
              style={{
                position: 'absolute',
                left: timeToPx(a.start),
                width: timeToPx(a.duration),
                height: 20,
                background: '#a64',
                borderRadius: 3,
                fontSize: 10,
                paddingLeft: 4,
              }}
            >
              {a.mediaId}
            </div>
          ))}
        </div>
        {/* playhead */}
        <div
          style={{
            position: 'absolute',
            top: 0,
            bottom: 0,
            left: timeToPx(time),
            width: 2,
            background: 'red',
            pointerEvents: 'none',
          }}
        />
      </div>
    </div>
  );
}
```

`ui/src/App.tsx` 改為：

```tsx
import { useProject } from './stores/project.js';
import { Player } from './player/Player.js';
import { Timeline } from './timeline/Timeline.js';

export function App() {
  const { doc, version, connected } = useProject();
  return (
    <div style={{ padding: 16, display: 'flex', flexDirection: 'column', gap: 12 }}>
      <p style={{ margin: 0 }}>
        {connected ? '🟢' : '🔴'} {doc?.name ?? '—'} v{version}
      </p>
      <Player />
      <Timeline />
    </div>
  );
}
```

- [ ] **Step 4: 跑測試 + type check**

Run: `npm run test -w @vidcut/ui && npx tsc --noEmit -p ui`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add ui/src
git commit -m "feat(m1): read-only timeline with filmstrip, waveform, playhead, click-seek"
```

---

### Task 11: demo 專案建置腳本

**Files:**

- Create: `server/src/demo.ts`
- Modify: root `package.json`（加 script `demo`）、`.gitignore`（加 `projects/`）
- Test: `server/test/demo.test.ts`

**Interfaces:**

- Consumes: `makeVideo`（Task 5 fixtures 的邏輯——demo.ts 內建同款 lavfi 產生器，不 import test 檔）、`ingestMedia`（Task 6）、`ProjectStore`（Task 3）。
- Produces: `buildDemoProject(dir: string): Promise<void>` — 在 `dir` 產 5 支不同顏色/音頻的 lavfi 直式影片（3–6 秒不等，其中 1 支無音軌）→ ingest 全部 → `set` 5 個 clip（各取中段 2–4 秒）→ 1 個常駐 overlay PNG（ffmpeg 產一張 640×200 標題圖：`color=c=black@0.0,drawtext=...` 或直接 lavfi `color` + drawtext 輸出 png）→ 2 條字幕。root script：`npm run demo` → 建置 `projects/demo` 後啟動 server。

- [ ] **Step 1: 寫失敗測試**

`server/test/demo.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { mkdtemp } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { buildDemoProject } from '../src/demo.js';
import { ProjectStore } from '../src/store.js';

describe('buildDemoProject', () => {
  it('creates a playable 5-clip project with overlay and captions', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'vidcut-demo-'));
    await buildDemoProject(dir);
    const store = await ProjectStore.load(join(dir, 'project.json'));
    expect(store.doc.media.length).toBe(5); // 5 支影片；overlay PNG 走 imagePath 直接引用，不進 media
    expect(store.doc.tracks.video).toHaveLength(5);
    expect(store.doc.tracks.overlays.length).toBeGreaterThan(0);
    expect(store.doc.tracks.captions.length).toBeGreaterThan(0);
    for (const m of store.doc.media) {
      if (m.probe) expect(m.proxyPath).toBeTruthy();
    }
  }, 180_000);
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npm run test -w @vidcut/server`
Expected: FAIL（demo.ts 不存在）

- [ ] **Step 3: 實作 `server/src/demo.ts`**

```ts
import { join } from 'node:path';
import { mkdir } from 'node:fs/promises';
import { ProjectStore } from './store.js';
import { ingestMedia } from './ingest.js';
import { runFfmpeg } from './ffmpeg.js';
import { nanoid } from 'nanoid';

const CLIPS = [
  { name: 'n5.mp4', dur: 5, freq: 330, mute: false },
  { name: 'n4.mp4', dur: 4, freq: 392, mute: false },
  { name: 'n3.mp4', dur: 6, freq: 440, mute: true }, // 無音軌 → 驗證 anullsrc 路徑
  { name: 'n2.mp4', dur: 5, freq: 494, mute: false },
  { name: 'n1.mp4', dur: 6, freq: 523, mute: false },
];

export async function buildDemoProject(dir: string): Promise<void> {
  await mkdir(dir, { recursive: true });

  for (const c of CLIPS) {
    const args = ['-f', 'lavfi', '-i', `testsrc2=duration=${c.dur}:size=540x960:rate=30`];
    if (!c.mute) args.push('-f', 'lavfi', '-i', `sine=frequency=${c.freq}:duration=${c.dur}`);
    args.push('-c:v', 'libx264', '-preset', 'ultrafast', '-pix_fmt', 'yuv420p');
    if (!c.mute) args.push('-c:a', 'aac', '-shortest');
    args.push(join(dir, c.name));
    await runFfmpeg(args);
  }

  // 常駐標題 overlay PNG
  await runFfmpeg([
    '-f',
    'lavfi',
    '-i',
    'color=c=black@0.0:size=800x160,format=rgba',
    '-vf',
    "drawtext=text='VIDCUT DEMO':fontsize=72:fontcolor=white:x=(w-text_w)/2:y=(h-text_h)/2:borderw=4:bordercolor=black",
    '-frames:v',
    '1',
    join(dir, 'title.png'),
  ]);

  const store = await ProjectStore.load(join(dir, 'project.json'));
  for (const [i, c] of CLIPS.entries()) {
    await ingestMedia(store, dir, c.name, { label: `No.${5 - i}` });
  }
  store.mutate('ai', 'demo timeline', (d) => {
    d.tracks.video = d.media.map((m, i) => ({
      id: nanoid(6),
      mediaId: m.id,
      in: 1,
      duration: Math.min(3, m.probe.duration - 1),
      label: m.label,
      volume: 1,
    }));
    d.tracks.overlays = [
      {
        id: nanoid(6),
        imagePath: 'title.png',
        start: 0,
        duration: null,
        position: { x: 0.5, y: 0.06, scale: 1 },
      },
    ];
    d.tracks.captions = [
      {
        id: nanoid(6),
        text: '第一段字幕',
        start: 1,
        duration: 3,
        style: { fontFamily: 'sans-serif', fontSize: 64, fill: '#fff', stroke: '#000', y: 0.78 },
      },
      {
        id: nanoid(6),
        text: '第二段字幕',
        start: 6,
        duration: 4,
        style: { fontFamily: 'sans-serif', fontSize: 64, fill: '#ff0', stroke: '#000', y: 0.78 },
      },
    ];
  });
  await store.flush();
}

// CLI: tsx src/demo.ts <dir>
import { fileURLToPath } from 'node:url';
import { resolve } from 'node:path';
const isMain = process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url);
if (isMain) {
  const dir = resolve(process.argv[2] ?? 'projects/demo');
  await buildDemoProject(dir);
  console.log(`demo project ready at ${dir}`);
}
```

root `package.json` scripts 加：

```json
"demo": "tsx server/src/demo.ts projects/demo && npm run dev:server -- projects/demo"
```

`.gitignore` 加一行 `projects/`。

- [ ] **Step 4: 跑測試確認通過**

Run: `npm run test -w @vidcut/server`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/demo.ts server/test/demo.test.ts package.json .gitignore
git commit -m "feat(m1): demo project builder (5 lavfi clips + overlay + captions)"
```

---

### Task 12: M1 驗收（手動）

**Files:** 無新檔案——依驗收結果修 bug（每個修復單獨 commit）。

**Interfaces:** 驗收 spec §12 M1 完成標準：「手寫 project.json，UI 能無縫播放 5 段 proxy + overlay」。

- [ ] **Step 1: 啟動**

```bash
npm run demo          # 終端機 A：建 demo 專案 + server（127.0.0.1:3845）
npm run dev:ui        # 終端機 B：http://localhost:5173
```

- [ ] **Step 2: 驗收清單（逐項確認）**

1. 開啟頁面：🟢 已連線、專案名 `demo`、時間軸顯示 5 個 clip（filmstrip 縮圖 + 底部綠色波形；No.3 無音軌 clip 波形應為平線）。
2. 按 ▶：從頭播到尾（約 15 秒），5 段連續播放，段落切換無黑幀、無明顯停頓（每段 testsrc2 畫面上的計時器與色塊變化可辨識切換點）。
3. overlay「VIDCUT DEMO」全程常駐頂部；兩條字幕在 1–4s 與 6–10s 出現、消失。
4. 音訊：各段 sine 音高不同、切換時無爆音斷裂；No.3 段靜音。
5. 點時間軸任意位置：playhead 跳過去、預覽畫面同步跳轉（scrub 反應 < 0.5s）。
6. 播放到片尾：自動停在最後一幀，▶ 變回可按。
7. 開第二個瀏覽器分頁：兩邊 playhead 各自獨立（播放是 client 端狀態），但專案內容一致。
8. 手改 `projects/demo/project.json` 不重啟 server 的驗證改走 API：`curl http://127.0.0.1:3845/api/project | head -c 300` 能看到目前文件。

- [ ] **Step 3: 已知可接受的 M1 限制（不算失敗）**

- 切換瞬間若偶有 <100ms 的畫面凍結（保留前一幀）可接受；黑幀/白閃不可接受（見 spec §13 風險：仍有 gap 時 M2 加 1 幀淡切緩解）。
- Safari 未測（開發用 Chrome）。
- 字幕字級與最終渲染尺寸未逐像素對齊（M2 處理）。

- [ ] **Step 4: 全部通過後打 tag**

```bash
git tag m1-done
```

---

## 驗收後

M1 完成後回報使用者，展示 demo，然後為 M2「改得動」（trim 拖拉、排序、overlay/caption 編輯、undo UI、活動面板）寫下一份計畫。
